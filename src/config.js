
const api_host = "http://localhost:5123";

export default {
  reload_file_list_url: api_host + "/loadFile",
  read_file_list: api_host + "/nameList",
  save_market_list: api_host + "/saveFileMarketplace"
}